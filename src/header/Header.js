import "./Header.css"

 function Header(){
    return(
        <header className="mainheader">
            <h4 className="subHeading">EMPLOYEE CRUD</h4>
        </header>
    );
}

export default Header